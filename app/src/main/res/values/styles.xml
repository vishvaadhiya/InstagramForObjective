<resources>

    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/sky</item>
        <item name="colorPrimaryDark">@color/sky</item>
        <item name="colorAccent">@color/sky</item>
        <item name="background">@drawable/ic_background</item>
    </style>

    <style name="Base.Theme.InstagramForObjective" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@color/sky</item>
        <item name="android:colorBackground">@color/sky</item>
        <item name="android:windowBackground">@drawable/ic_background</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!--<style name="Theme.InstagramForObjective" parent="Base.Theme.InstagramForObjective" />-->
    <style name="RoundedButton" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="background">@color/sky</item>
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>

    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:statusBarColor">@color/transparent</item>
        <item name="android:navigationBarColor">@color/transparent</item>
        <item name="android:windowBackground">@drawable/background_splash</item>
    </style>

    <style name="capsuleButton" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:textColor">@color/white</item>
        <item name="cornerRadius">@dimen/_20sdp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:drawablePadding">@dimen/_5sdp</item>
        <item name="android:paddingStart">@dimen/_20sdp</item>
        <item name="android:paddingTop">@dimen/_10sdp</item>
        <item name="android:paddingBottom">@dimen/_10sdp</item>
        <item name="android:paddingEnd">@dimen/_20sdp</item>
    </style>


</resources>